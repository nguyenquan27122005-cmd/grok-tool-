# Note

Pipeline chính nằm trong **`D:\grok_tool\grok_tool`**:

`reg hotmail → Grok free → auto add Sub2API`

- Code: `grok_tool/main.py` + `grok_tool/sub2api_oauth.py`
- Config: `grok_tool/config.json` → key `sub2api`

Folder này giữ CLI batch (re-import acc `success` cũ). Ưu tiên chạy:

```powershell
cd D:\grok_tool\grok_tool
.\venv\Scripts\Activate.ps1
python main.py
```
