Ledger `accounts.txt`: `email|password|status|time|note`

`last_session.json` — cookie/session sau Chrome (kèm `stripe_url` nếu claim được).
`last_protocol.json` — kết quả HTTP / B2C probe.
`last_page.json` — snapshot DOM lúc fail.
`last_captcha.png` — ảnh CAPTCHA B2C vừa gửi solver.
